// git log helps us to see histroy of all commit, when it was committed and who committed it and the commit messge

// to revert to a previous commit  you use git checkout (hash of commit which is gotten from log)

